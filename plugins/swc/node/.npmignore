# Ignore source files and config
src/
test/
tsconfig.json
pnpm-lock.yaml

# Explicitly include the WASM directory and binary
# This overrides the root .gitignore during npm publish
!wasm/
!wasm/*.wasm
