query ReferenceQuery($first: Int = 10, $after: String) {
  me {
    ...UserFields
    unmappedField
  }
  posts(first: $first, after: $after) {
    nodes {
      ...PostFields
    }
    pageInfo {
      hasNextPage
      endCursor
    }
    totalCount
  }
}
